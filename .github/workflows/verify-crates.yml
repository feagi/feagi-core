# Copyright 2025 Neuraville Inc.
# SPDX-License-Identifier: Apache-2.0

name: Verify Individual Crates

on:
  pull_request:
    branches: [ main, staging ]
    paths:
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'

env:
  CARGO_TERM_COLOR: always

jobs:
  verify-crate-structure:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
    
    - name: Rust Cache
      uses: Swatinem/rust-cache@v2
      with:
        shared-key: "verify-crates"
        save-if: true
    
    - name: Verify all crate metadata
      run: |
        echo "🔍 Verifying crate metadata for crates.io publication..."
        echo ""
        
        FAILED_CRATES=()
        
        for crate_dir in crates/*/; do
          if [ ! -f "$crate_dir/Cargo.toml" ]; then
            continue
          fi
          
          crate_name=$(basename "$crate_dir")
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Checking: $crate_name"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          CRATE_FAILED=false
          
          # Check required fields
          if ! grep -q '^name = ' "$crate_dir/Cargo.toml"; then
            echo "  ❌ Missing 'name' field"
            CRATE_FAILED=true
          fi
          
          # Check version (direct or workspace inheritance)
          if ! grep -qE '^version(\.workspace)? = ' "$crate_dir/Cargo.toml"; then
            echo "  ❌ Missing 'version' field"
            CRATE_FAILED=true
          fi
          
          if ! grep -q '^description = ' "$crate_dir/Cargo.toml"; then
            echo "  ❌ Missing 'description' field (required for crates.io)"
            CRATE_FAILED=true
          fi
          
          # Check license (direct or workspace inheritance)
          if ! grep -qE '^license(\.workspace)? = ' "$crate_dir/Cargo.toml"; then
            echo "  ❌ Missing 'license' field (required for crates.io)"
            CRATE_FAILED=true
          fi
          
          # Check authors (direct or workspace inheritance)
          if ! grep -qE '^authors(\.workspace)? = ' "$crate_dir/Cargo.toml"; then
            echo "  ❌ Missing 'authors' field (required for crates.io)"
            CRATE_FAILED=true
          fi
          
          # Check for path dependencies (should use workspace dependencies)
          if grep -q 'path = ".*/crates/' "$crate_dir/Cargo.toml" 2>/dev/null; then
            echo "  ⚠️  Warning: Found absolute path dependencies (should use relative paths)"
          fi
          
          # Check for README
          if [ ! -f "$crate_dir/README.md" ]; then
            echo "  ⚠️  Warning: No README.md (recommended for crates.io)"
          fi
          
          if [ "$CRATE_FAILED" = true ]; then
            FAILED_CRATES+=("$crate_name")
            echo "  ❌ $crate_name FAILED metadata check"
          else
            echo "  ✅ $crate_name has valid metadata"
          fi
          
          echo ""
        done
        
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📊 Metadata Verification Summary"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        
        if [ ${#FAILED_CRATES[@]} -gt 0 ]; then
          echo "❌ Failed crates: ${#FAILED_CRATES[@]}"
          echo ""
          echo "Crates with missing metadata:"
          for crate in "${FAILED_CRATES[@]}"; do
            echo "  - $crate"
          done
          echo ""
          exit 1
        else
          echo "✅ All crates have required metadata"
        fi
    
    - name: Verify individual crate builds
      run: |
        echo "🔨 Building each crate individually..."
        echo ""
        
        FAILED_BUILDS=()
        
        for crate_dir in crates/*/; do
          if [ ! -f "$crate_dir/Cargo.toml" ]; then
            continue
          fi
          
          crate_name=$(basename "$crate_dir")
          echo "Building $crate_name..."
          
          if (cd "$crate_dir" && cargo build --lib --quiet 2>&1); then
            echo "  ✅ $crate_name builds successfully"
          else
            echo "  ❌ $crate_name failed to build"
            FAILED_BUILDS+=("$crate_name")
          fi
        done
        
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        
        if [ ${#FAILED_BUILDS[@]} -gt 0 ]; then
          echo "❌ Failed to build: ${#FAILED_BUILDS[@]} crates"
          for crate in "${FAILED_BUILDS[@]}"; do
            echo "  - $crate"
          done
          exit 1
        else
          echo "✅ All crates build successfully"
        fi
    
    - name: Verify crate packaging (dry-run publish check)
      run: |
        echo "📦 Verifying each crate can be packaged for publication..."
        echo ""
        
        FAILED_PACKAGES=()
        
        for crate_dir in crates/*/; do
          if [ ! -f "$crate_dir/Cargo.toml" ]; then
            continue
          fi
          
          crate_name=$(basename "$crate_dir")
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Packaging: $crate_name"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          if (cd "$crate_dir" && cargo package --quiet --allow-dirty 2>&1); then
            echo "  ✅ $crate_name packages successfully"
          else
            echo "  ❌ $crate_name failed to package"
            FAILED_PACKAGES+=("$crate_name")
            
            # Show detailed error
            echo ""
            echo "Detailed error:"
            (cd "$crate_dir" && cargo package --allow-dirty 2>&1) || true
            echo ""
          fi
        done
        
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📊 Packaging Verification Summary"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        
        if [ ${#FAILED_PACKAGES[@]} -gt 0 ]; then
          echo "❌ Failed to package: ${#FAILED_PACKAGES[@]} crates"
          echo ""
          echo "Crates that cannot be published:"
          for crate in "${FAILED_PACKAGES[@]}"; do
            echo "  - $crate"
          done
          echo ""
          echo "These crates need to be fixed before they can be published to crates.io."
          exit 1
        else
          echo "✅ All crates can be packaged successfully"
          echo "✅ Ready for publication to crates.io"
        fi
    
    - name: Verify dependency versions
      run: |
        echo "🔗 Verifying workspace dependency versions..."
        echo ""
        
        # Get workspace version
        WORKSPACE_VERSION=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
        echo "Workspace version: $WORKSPACE_VERSION"
        echo ""
        
        MISMATCHED=()
        
        for crate_dir in crates/*/; do
          if [ ! -f "$crate_dir/Cargo.toml" ]; then
            continue
          fi
          
          crate_name=$(basename "$crate_dir")
          crate_version=$(grep '^version = ' "$crate_dir/Cargo.toml" | head -1 | sed 's/version = "\(.*\)"/\1/')
          
          if [ -n "$crate_version" ] && [ "$crate_version" != "$WORKSPACE_VERSION" ]; then
            echo "  ⚠️  $crate_name version mismatch: $crate_version (workspace: $WORKSPACE_VERSION)"
            MISMATCHED+=("$crate_name: $crate_version")
          fi
        done
        
        if [ ${#MISMATCHED[@]} -gt 0 ]; then
          echo ""
          echo "⚠️  Warning: ${#MISMATCHED[@]} crates have different versions"
          echo "This is allowed for hybrid versioning, but verify it's intentional."
          echo ""
          for item in "${MISMATCHED[@]}"; do
            echo "  - $item"
          done
        else
          echo "✅ All crates use workspace version: $WORKSPACE_VERSION"
        fi
    
    - name: Summary
      if: success()
      run: |
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ All Crate Verification Checks Passed"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "✅ Metadata complete for all crates"
        echo "✅ All crates build independently"
        echo "✅ All crates package successfully"
        echo "✅ Dependency versions verified"
        echo ""
        echo "🎉 Workspace is ready for publication to crates.io"


