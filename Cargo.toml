# ============================================================================
#              Combo Library Crate and Workspace Cargo File
# ============================================================================

# NOTE: We make use of cfg compile flags to targets specific systems.
# See https://doc.rust-lang.org/reference/conditional-compilation.html#r-cfg.conditional
# TODO While embedded is under cfg target_os "none", but we need features to differentiate between embedded hardwares



# ============================================================================
#                          LIBRARY CONFIGURATION
# ============================================================================

[package]
name = "feagi"
version = "0.0.12"
edition = "2021"
authors = ["Neuraville Inc. <feagi@neuraville.com>"]
license = "Apache-2.0"
description = "Framework for Evolutionary Artificial General Intelligence - Bio-Inspired Neural Computation"
repository = "https://github.com/feagi/feagi-core"
readme = "README.md"
keywords = ["neuroscience", "ai", "neural-network", "brain", "agi"]
categories = ["science", "simulation"]

[lints]
workspace = true

[features]
default = ["default-config-feagi-server"]

# ========================= Common Configuration Sets ========================= #
# Common classes of FEAGI Applications using the crates of this library

# Default features for compiling the FEAGI Server
default-config-feagi-server = [
    #"feagi-agent/agent-server",
    "agent-transport-zmq",
    "agent-transport-websocket-std",
    "agent-transport-shm",
    "agent-transport-bluetooth",
    "agent-server",
    "use-rayon",
    "std",
]

# Default features for a FEAGI Agent (an embodiement, like a robot or game avatar) with std support (AKA not embedded).
# Note the transport protocol method will need to be specified!
default-config-feagi-agent-std = [
    #"agent-transport-zmq",
    #"agent-transport-websocket-std",
    #"agent-transport-shm",
    #"agent-transport-bluetooth",
    #"agent-client-asynchelper-tokio"
    "agent-client",
    "use-rayon",
    "std",
]

# Default features for a FEAGI Agent (an embodiement, like a robot or game avatar) without std.
# Note the transport protocol method will need to be specified!
default-config-feagi-agent-embedded = [
    #"agent-client",
    #"agent-transport-zmq",
    #"agent-transport-websocket-std",
    #"agent-transport-shm",
    #"agent-transport-bluetooth",
    #"agent-client-asynchelper-tokio"
]

# Default features for a FEAGI Genome Editor (mainly Brain Visualizer)
default-config-feagi-editor = [
    #"agent-client",
    "agent-transport-websocket-std",
    "use-rayon",
    "std",
]

# Compiles all feagi crates, mainly for creating wrappers in other languages. Does not include optional features!
default-config-feagi-language-lib = [
    # TODO
]

# ========================= Toggleable Features ========================= #
# Features that add certain libraries/crates that extend the functionality
# and performance of the FEAGI application
# In order of other features here, feagi crate features


# TODO These features should activate subcrate features

# Allows use of mutlithreading to accomplish large parallel tasks faster. Requires std.
use-rayon = [
    "std",
]

# Allows conversion of some FEAGI structs to / from NDArrays.
use-ndarray = [

]

use-image = []



# Specifies this is a client of a FEAGI server
agent-client = []

# Specifies this is a server running FEAGI
agent-server = []

# Transport types that can be used by the compiled program.
agent-transport-zmq = [
    #"feagi-agent/agent-client"
]
agent-transport-websocket-std = [
    "std",
    #"feagi-agent/agent-transport-websocket-std"
]
agent-transport-shm = [
    #"feagi-agent/agent-transport-shm"
]
agent-transport-bluetooth = [
    #"feagi-agent/agent-transport-bluetooth"
]

# Use the Rust Standard Library. Needs to be disabled by some compile targets. Enables alloc.
std = [
    "alloc",
]

# Allow the use of allocation (such as with vectors, hashmaps). Needs to be disabled by some compile targets.
alloc = [

]

# TODO do we need wasm specifically? we may need a feature for wasm anyways due to wasm_sync of rayon and others


[dependencies]
feagi-logging-and-errors = {path = "crates/feagi-logging-and-errors"}
feagi-data = {path = "crates/feagi-data"}





# ============================================================================
#                             WORKSPACE CONFIGURATION
# ============================================================================

[workspace]
resolver = "2"
members = [
    # === Errors and Logs ===
    "crates/feagi-logging-and-errors", # Base FEAGI Error and logging macros and trait definitions, Since everything depends on this, this is effectively the root!
    "crates/feagi-logging-and-errors/feagi-logging-and-errors-derive", # derive crate for logging and errors

    # === Base Data Types ===
    "crates/feagi-data", # Holds common data structures that are the backbone of many data operations of FEAGI

    # === Genome ===
    "crates/feagi-genomic", # Crate that holds genomic subcrates
    "crates/feagi-genomic/feagi-genomic-context", # Contains Identifiers and enums to identify specific genome structures
    "crates/feagi-genomic/feagi-genomic-data", # Structures to describe the state of genome structures, used as high level connectome abstractions

    # === Neuron Processing ===
    "crates/feagi-npu", # Crate that holds NPU Subcrates
    "crates/feagi-npu/feagi-npu-common", # Common structs and functions used by other NPU subcrates
    "crates/feagi-npu/feagi-npu-models", # Definitions for the neuron and synapse biological simulation models
    "crates/feagi-npu/feagi-npu-dynamics", # Systems for modifying the connectome at runtime and validating changes


    # === Algorithm Layer ===
    #"crates/feagi-agent", # Agent library (Rust client library)

    #"crates/feagi-structures/feagi-genome-definitions", # Holds structs and definitions of various genome identifiers or common definitions used acrosss crates, such as CorticalID. Also holds definitions for cortical_area units

    #"crates/feagi-config",             # Configuration loader (TOML + overrides)


    
    # === Platform HALs ===
    #"crates/feagi-hal",                # Hardware Abstraction Layer (HAL) - platform abstraction + implementations (ESP32, Arduino, etc.)
    
    # === Infrastructure Layer (Cross-Cutting) ===
    #"crates/feagi-state-manager",      # Runtime state management
    #"crates/feagi-evolutionary",                # Genome I/O and evolution (genotype)
    #"crates/feagi-services",           # Service layer (stable application boundary)
    #"crates/feagi-api",                # REST API layer (HTTP + ZMQ transports)
    #"crates/feagi-observability",      # Unified observability (logging, telemetry, profiling)
    
    # === Algorithm Layer ===
    #"crates/feagi-brain-development",                # Neurogenesis (brain development)
    
    # === Agent Layer ===
    #"crates/feagi-io",                 # I/O System (ZMQ/UDP transport)
    #"crates/feagi-agent",              # Agent library (Rust client library)
    #"crates/feagi-sensorimotor",  # Sensorimotor System - data processing & encoding (sensory & motor)
    #"crates/feagi-serialization", # Data serialization formats
]

[workspace.package]
version = "0.0.12"
edition = "2021"
rust-version = "1.75"
authors = ["Neuraville Inc. <feagi@neuraville.com>"]
license = "Apache-2.0"
repository = "https://github.com/feagi/feagi-core"
homepage = "https://feagi.org"
documentation = "https://docs.rs/feagi"


[workspace.dependencies] # Contains dependencies that several crates use
rayon = {version = "1.12.0", default-features = false}
ndarray = { version = "0.17.2", default-features = false}
serde = { version = "1.0.228", default-features = false}
serde_json = { version = "1.0.150", default-features = false}
paste = { version =  "1.0.15" , default-features = false}
ahash = { version = "0.8.12", default-features = false, features = ["std", "serde", "compile-time-rng"] }  # Fast non-cryptographic hashing (no runtime-rng for WASM) # TODO remove features


# TODO update and review below
image = { version = "0.25.10" } # TODO this shouldnt be here, this should be in the feagi sensiomotor specifically
proc-macro2 = "1.0.106"
quote = "1.0.45"
syn = { version = "2.0.117", features = ["full"] }
thiserror = "1.0.50" # TODO migrate away from this and use our own error trait!

tracing = "0.1"  # Structured logging and diagnostics # TODO shouldnt this only be in the error subcrate?
defmt = {version = "1.1.0"} # only used in embedded contexts # TODO shouldnt this only be in the error subcrate?
half = {version = "2.7.1", default-features = false}
uuid = { version = "1.11", features = ["v7", "serde", "js"] }  # js feature for WASM supporto


[workspace.lints.rust]
# TODO for Phase 2



[profile.release]
lto = true           # Link-time optimization
codegen-units = 1    # Better optimization, slower compile
opt-level = 3        # Maximum optimization
panic = "abort"      # Smaller binary, no unwinding

[profile.dev]
opt-level = 1        # Some optimization in dev for reasonable performance

[profile.benchmark]
inherits = "release"
lto = true
codegen-units = 1
opt-level = 3
debug = true

[profile.test]
opt-level = 0
debug = true

# ============================================================================
# BINARY TOOLS
# ============================================================================

# TODO review these

[[bin]]
name = "migrate_genome"
path = "tools/migrate_genome.rs"
required-features = []

[[bin]]
name = "perf_compare"
path = "tools/perf_compare.rs"
required-features = []
