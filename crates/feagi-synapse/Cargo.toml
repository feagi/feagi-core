[package]
name = "feagi-synapse"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Platform-agnostic synaptic computation algorithms for FEAGI"
publish = false

[features]
default = []
std = []

[dependencies]
# Pure no_std dependencies only
feagi-types = { path = "../feagi-types" }

[dev-dependencies]
# For tests only


