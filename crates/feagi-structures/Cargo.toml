[package]
name = "feagi-structures"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "The most core library, defines the basic data types used by FEAGI, as well as some processors to modify them"

[features]
default = []
no-std = [] # Disables all structures dependent on the Standard Library. Mainly needed for embedded builds

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true}
ndarray = { workspace = true}
rayon = { workspace = true}
uuid = { workspace = true }
paste = "1.0.15"
base64 = "0.22.1"

